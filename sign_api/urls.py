from django.conf.urls import url
from sign_api import views

urlpatterns = [
    # sign system interface:
    # ex : /api/add_event/
    # url(r'^add_event/', views.add_event, name='add_event'),
    # ex : /api/add_guest/
    # url(r'^add_guest/', views.add_guest, name='add_guest'),
    # ex : /api/get_event_list/
    # url(r'^get_event_list/', views.get_event_list, name='get_event_list'),
    # ex : /api/get_guest_list/
    # url(r'^get_guest_list/', views.get_guest_list, name='get_guest_list'),
    # ex : /api/user_sign/
    # url(r'^user_sign/', views.user_sign, name='user_name'),
]